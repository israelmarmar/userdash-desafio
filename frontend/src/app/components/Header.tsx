import Image from 'next/image';
import React from 'react';

const Header = () => {
    return (
        <header className='header'>
            UserDash
        </header>
    );
};

export default Header;
